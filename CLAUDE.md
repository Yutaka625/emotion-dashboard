# KSDV（emotion-dashboard）プロジェクト ルール

このファイルは、KSDV（感情分析ダッシュボード）リポジトリでの作業ルールを定める。
ワークスペース全体のルールは親ディレクトリの `Projects/CLAUDE.md` を参照すること。

---

## ドキュメント整合性チェックのルール（必須）

**KSDV（アプリ本体）の機能・UI・出力仕様を変更したときは、以下の2つの資料との整合性を必ず確認すること。**

- **ユーザーガイド**: `KSDV_User-Guide.html`
  （リポジトリ直下版 ＋ `client/public/KSDV_User-Guide.html` の同期版。アプリ内の「ガイド」ボタンのリンク先）
- **技術説明資料**: `KSDV（感情分析ダッシュボード）技術説明資料.md`

### チェックの手順
1. コード変更が上記資料の記載内容に影響するかを確認する。
   - 主な観点：画面・タブ構成／指標の定義・計算式・しきい値／CSV出力の列・セクション構成／
     統計手法／ベースライン補正の挙動／バージョン表記 など
2. 不整合・記載漏れ・古い記述を見つけた場合は、**該当箇所と具体的な修正案をユーザーに打診する**。
   - 資料を勝手に大きく書き換えない（対外配布・公開される可能性があるため、正確性とユーザー判断を優先）。
   - 明らかな事実誤り（数値・列名の誤記等）は、修正案の提示とあわせて直してよい。
3. ユーザーガイドを更新する場合は、**リポジトリ直下版と `client/public/` 版の両方を必ず同期**する。

### 補足
- 法的文書（`KSDV_terms.html`）は本ルールの対象外。変更時は社名・条項・プレースホルダー等を別途確認する。
- ガイド／技術資料を更新した場合も、コミット前に両資料間で齟齬がないか相互に確認する。
