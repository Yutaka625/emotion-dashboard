# KSDV ダッシュボード 画面ガイド

> 各タブのセクション（ブロック）の内容・数字・グラフが何を意味するかを、具体例を交えて解説します。

---

## 目次

1. [概要（Overview）](#1-概要-overview)
2. [時系列分析（Timeseries）](#2-時系列分析-timeseries)
3. [特殊指標（Engagement / Valence）](#3-特殊指標-engagement--valence)
4. [感情分布（Emotions）](#4-感情分布-emotions)
5. [感情遷移（Transitions）](#5-感情遷移-transitions)
6. [学術的分析（Academic）](#6-学術的分析-academic)
7. [アクションユニット（Action Units）](#7-アクションユニット-action-units)
8. [UXリサーチ（UX Research）](#8-uxリサーチ-ux-research)
9. [セッション比較（Comparison）](#9-セッション比較-comparison)
10. [付録：学術研究・研究開発での活用ガイド](#付録学術研究研究開発での活用ガイド)

---

## マルチFaceID機能

複数人のデータが含まれるCSVファイル（FaceID列あり）をアップロードすると、画面上部に **FaceID切り替えUI** が表示されます。

- **「全員」ボタン：** 全FaceIDのデータを合算して表示
- **個別チップ：** クリックで特定の人物のみを表示
- **Ctrl/Cmd+クリック：** 複数人を選択して合算表示

FaceID列がない、または1人分のデータのみの場合は通常モードで動作します。

---

## ベースライン補正

ベースラインセッション（刺激なしの安静状態）のCSVを別途アップロードすることで、感情スコアから安静時の基準値を差し引いた「補正済みスコア」で全セクションを表示できます。補正が有効な間は画面上部にバナーが表示されます。

---

## 1. 概要（Overview）

ファイルをアップロードした直後に表示されるサマリー画面です。セッション全体の「要約」を一覧で確認できます。

---

### SESSION OVERVIEW（セッション概要）

6つの主要指標がカード形式で並びます。

| 表示ラベル | 意味 | 例 |
|---|---|---|
| **総フレーム数** | 分析対象のデータ行数（1行 = 1フレーム） | 18,000 frames |
| **記録時間** | データの合計時間（秒） | 600.0 sec |
| **平均Attention** | 視線・注意の向きの平均スコア（0〜100%） | 72.3% |
| **平均Engagement** | 関与度の平均スコア（0〜100%） | 45.1% |
| **支配的感情** | セッション全体で最も多く出現した感情 | 喜び |
| **感情多様性** | 出現した感情の種類数 | 7 種類 |

> **例：** 総フレーム数18,000、記録時間600秒の場合、1秒あたり30フレーム（30fps）のデータが記録されています。

---

### 支配的感情の分布（ドーナツチャート）

セッション全体を通じて、どの感情が「支配的感情」として最も多く出現したかを割合で示します。

- **支配的感情** = そのフレームで最もスコアが高かった感情
- 例：「喜び 42%、ニュートラル 31%、驚き 12%」→ セッションの約4割は喜びが最も強かった

---

### EMOTION PROFILE（レーダーチャート）

10種類の感情スコアの平均値を正多角形のレーダーチャートで表示します。

- 各頂点が1つの感情を表し、外側に伸びるほどスコアが高い
- 例：「喜び」の頂点が大きく外側に出ていれば、喜びの平均スコアが高い
- 全体的に小さく丸い形 → 感情の起伏が少ない穏やかなセッション
- 特定の頂点だけ突出 → その感情が特徴的に強いセッション

---

### KEY INSIGHTS（キーインサイト）

アルゴリズムが自動生成する3〜5個の要約コメントです。

- 例：「喜びスコアが平均より高く、ポジティブなセッションでした」
- 例：「Engagementの変動が大きく、集中度にムラがありました」

---

### DATA QUALITY（データ品質）

CSVデータの品質指標を3つのメトリクスで表示します。

| 指標 | 意味 |
|---|---|
| **顔検出率** | 顔データが存在するフレームの割合 |
| **フレームレート** | 1秒あたりのフレーム数（fps） |
| **感情検出率** | 少なくとも1つの感情スコアが0より大きかったフレームの割合 |

---

## 2. 時系列分析（Timeseries）

感情スコアの「時間的な変化」を折れ線グラフで可視化するタブです。「いつ」「どの感情が」「どのくらい強かったか」を確認できます。

---

### EVENT ANNOTATIONS（イベント登録）

グラフ上に「介入区間」を色付きの縦線で重ねて表示する機能です。

- **使い方：** イベント名・開始時間（秒）・終了時間（秒）を入力して「追加」をクリック
- **例：** 「プレゼン開始」を 60〜300秒 に登録すると、グラフ上の60秒と300秒に縦線が引かれる
- 登録後は「EVENT EMOTION STATS」ブロックが自動生成され、その区間の感情統計が表示される
- 複数のイベントを登録して、区間ごとの反応を比較することができる
- 登録したイベントはUXリサーチセクションのタスク別分析にも連携される

---

### TIME RANGE FILTER（時間範囲フィルター）

分析対象の時間範囲を「開始秒」「終了秒」で絞り込む機能です。

- 入力後、以降のすべてのグラフ・統計がその範囲のデータに絞られる
- 例：「60〜120秒」を指定 → プレゼンの最初の1分間だけを集中分析
- **CSV出力ボタン：** 指定した時間範囲のデータを `emotion_data_60-120s.csv` のような名前でダウンロード可能。出力には感情スコア（9種、ニュートラルを除く）・engagement・valence・attention・AU01〜AU45（数値コード）が含まれる

---

### BASELINE SETTINGS（ベースライン補正後データ出力）

ベースライン補正が有効な間、BASELINE SETTINGSカードに **「補正後データを出力」ボタン** が表示されます。

- 出力形式: BOM付きCSV（Excel で文字化けしない形式）
- ファイル名に補正中心方式・表示モード・区間・出力日時を含む（例: `baseline_mean_deviation_0-30s_20260601-1430.csv`）
- ファイルは3つのセクションで構成される:

| セクション | 内容 |
|---|---|
| `## BASELINE CORRECTION METADATA` | 補正区間（秒）・中心方式（平均/中央値）・表示モード・出力日時 |
| `## BASELINE OFFSETS` | 指標ごとのオフセット値（offset）と標準偏差（sd） |
| `## CORRECTED TIMESERIES` | 補正後の全フレームデータ（time + 9感情 + engagement/valence/attention） |

> **注意：** AUはベースライン補正の対象外のため、補正後データ出力には含まれません。

---

### SPECIAL METRICS（特殊指標サマリー）

Engagement・Valence・Attentionの3指標の平均値をカードで表示します。

| 指標 | 意味 | 範囲 |
|---|---|---|
| **Engagement** | 関与度・集中度 | 0〜100% |
| **Valence** | 感情の快/不快の度合い | 0〜100%（50が中立） |
| **Attention** | 視線・注意の向き | 0〜100% |

---

### EMOTION TIME SERIES（感情時系列グラフ）

10種類の感情スコアの時間推移を折れ線グラフで表示します。

- **横軸：** 時間（秒）
- **縦軸：** 感情スコア（0〜100%）
- 各感情は固有の色で表示（喜び=カナリアイエロー、怒り=スカーレット、悲しみ=シアン、困惑=フォグブルーなど）
- グラフ上部のチェックボックスで表示する感情を選択可能
- **例：** 60〜90秒にかけて「喜び」が急上昇 → その区間に何らかのポジティブな刺激があった可能性

---

### DOMINANT EMOTION TIMELINE（支配的感情タイムライン）

各フレームで「最も強かった感情」を色付きのバーで時系列に表示します。

- 横軸が時間、縦の色帯が支配的感情の種類を示す
- 例：黄色（喜び）が長く続いた後、赤（怒り）に切り替わる → 感情の転換点が視覚的に把握できる

---

### 10-SECOND WINDOWS TABLE（10秒ウィンドウ集計表）

セッションを10秒ごとに区切り、各区間の感情スコア平均値を表形式で表示します。

- 例：「60〜70秒」の行を見ると、その10秒間の喜び・怒り・悲しみなどの平均スコアが確認できる
- 時系列グラフで見えにくい細かい変化を数値で確認したい場合に有用

---

## 3. 特殊指標（Engagement / Valence）

**Engagement（関与度）** と **Valence（感情価）** の2つの特殊指標を深掘り分析するタブです。タブ切り替えで両指標を行き来できます。これらは通常の感情スコアとは異なる複合的な指標です。

> **Engagement（関与度）とは：** 顔の動き・表情の複雑さ・目の動きなどを総合して算出される「どれだけ積極的に関与しているか」を示す指標。0〜100%で表され、高いほど関与度が高い。
>
> **Valence（感情価）とは：** 感情の「快・不快」の度合いを示す指標。50%が中立で、それより高いとポジティブ、低いとネガティブな感情状態を示す。

---

### 平均値・最大値・中央値・標準偏差（4つの数値カード）

Engagement（またはValence）の基本統計量を4つのカードで表示します。

| 指標 | 意味 | 例 |
|---|---|---|
| **平均値** | セッション全体の平均 | 45.23% |
| **最大値** | セッション中で最も高かった値 | 89.41% |
| **中央値** | 全フレームを並べたときの真ん中の値（外れ値に強い） | 43.10% |
| **標準偏差** | 「ばらつき」の大きさ | 18.72% |

> **例：** 平均45%・標準偏差18%の場合、多くのフレームで27〜63%の範囲にEngagementが収まっていたことを意味します。標準偏差が大きいほど、集中度の波が激しかったといえます。

---

### DISTRIBUTION（レベル分布）

Engagement（またはValence）を5段階（非常に低い・低い・中程度・高い・非常に高い）に分類し、各レベルのフレーム数を棒グラフで表示します。

---

### CORRELATIONS（各感情との相関係数）

Engagement（またはValence）と各感情スコアの相関係数を横棒グラフで表示します。

- `+1.0` に近い → Engagementが高いとき、その感情も高い傾向
- `-1.0` に近い → Engagementが高いとき、その感情は低い傾向
- 例：「喜び +0.62」→ 関与度が高い場面では喜びも高い傾向がある

---

### ENGAGEMENT × VALENCE 散布図（Engagementタブのみ）

各フレームをX軸（Valence）・Y軸（Engagement）の2次元空間にプロットした散布図です。

- **各点 = 1フレーム**（先頭1,000点を表示）
- **点の色 = そのフレームの支配的感情**
- 右上に点が集まる → 高Engagement・高Valence（積極的かつポジティブな状態）

---

### EMOTION PROFILE（レベル別の感情プロファイル）

Engagementが「高い時」と「低い時」で、各感情スコアがどう違うかを比較棒グラフで表示します。

---

### CIRCUMPLEX MODEL（Valence-Arousal 円環モデル）

感情状態を「覚醒度（Arousal）」と「感情価（Valence）」の2軸で4つの象限に分類し、各象限のフレーム数を表示します。このモデルはRussell（1980）に基づき、Engagementを覚醒度の代理指標として使用しています。

| 象限 | 状態の意味 | 例 |
|---|---|---|
| **高覚醒 × 高Valence** | 活性化・興奮状態 | 楽しい議論、発見の瞬間 |
| **高覚醒 × 低Valence** | 怒り・不安状態 | 緊張、プレッシャー |
| **低覚醒 × 高Valence** | リラックス・満足状態 | 穏やかな集中、安心感 |
| **低覚醒 × 低Valence** | 疲労・抑うつ状態 | 飽き、疲れ |

---

### AFFECT DYNAMICS（動態指標）

Engagementの「時間的な変動パターン」を5つの指標で定量化します。

| 指標 | 意味 | 高い場合の解釈 |
|---|---|---|
| **変動性（SD）** | 全体的なばらつきの大きさ | 集中度の波が激しい |
| **不安定性（MSSD）** | 隣接フレーム間の急激な変化の頻度 | 集中度が頻繁に急変する |
| **慣性（AR1）** | 前のフレームの値が次のフレームに引き継がれる度合い（0〜1） | 一度高くなると高い状態が続きやすい |
| **レンジ** | 最大値 − 最小値 | 集中度の最大・最小の差が大きい |
| **平均絶対変化量** | 1フレームあたりの平均変化量 | 1フレームごとの変化が大きい |

---

## 4. 感情分布（Emotions）

10種類の感情スコアの統計的分布と出現パターンを分析するタブです。

対象感情：怒り・軽蔑・嫌悪・恐怖・喜び・悲しみ・驚き・感傷・困惑・ニュートラル

---

### 感情カード（10枚）

各感情の平均スコア・最大スコア・支配率を1枚のカードで表示します。

| 表示項目 | 意味 |
|---|---|
| **大きな数字（例：0.023）** | セッション全体の平均スコア（0〜100%） |
| **バー（横線）** | 最大スコアの相対的な大きさ |
| **支配: X.X%** | その感情が「支配的感情」だったフレームの割合 |

---

### EMOTION PREVALENCE（感情出現率）

各感情が「閾値以上のスコアを示したフレームの割合」を棒グラフで表示します。

- 平均スコアとは異なり、「その感情が有意に出現した時間の割合」を示す
- 例：「喜び 68%」→ セッションの68%の時間で喜びスコアが一定以上だった

---

### DESCRIPTIVE STATISTICS（記述統計表）

全感情の平均値・標準偏差・最大値・中央値を一覧表で表示します。

---

### AFFECT DYNAMICS COMPARISON（動態指標比較）

各感情の変動性・不安定性・慣性を比較する棒グラフです。複数の感情を並べて「どの感情が最も変動しやすいか」「どの感情が最も持続しやすいか」を比較できます。

---

## 5. 感情遷移（Transitions）

感情が「どの感情からどの感情へ変化したか」のパターンを分析するタブです。

---

### TRANSITION MATRIX（感情遷移行列）

10×10のマトリクス表で、感情間の遷移回数を色の濃さで表示します。

- **行（FROM）：** 遷移元の感情
- **列（TO）：** 遷移先の感情
- **セルの色の濃さ：** 遷移回数（濃いほど多い）。明度補間方式で視認性を確保
- **対角線（同じ感情→同じ感情）：** グレーで表示（同じ感情が連続したフレーム）

> **例：** 「喜び → ニュートラル」のセルが濃い → 喜びの後にニュートラルに戻ることが多いパターン。

---

### TOP TRANSITIONS（主要遷移パターン上位10件）

最も頻繁に発生した感情遷移パターンを棒グラフで表示します。

---

### DURATION ANALYSIS（持続時間分析）

各感情が1回の出現で「どのくらいの時間持続したか」を分析します。

| 表示項目 | 意味 |
|---|---|
| **出現回数** | その感情が支配的感情になった連続区間の数 |
| **平均持続時間** | 1回の出現で平均何秒続いたか |
| **最大持続時間** | 最長で何秒続いたか |

---

### TOTAL DURATION（合計持続時間）

各感情の合計持続時間（秒）を棒グラフで表示します。

---

### DURATION STATISTICS（持続時間統計表）

各感情の持続時間に関する詳細統計（平均・標準偏差・最大・合計）を一覧表で表示します。

---

## 6. 学術的分析（Academic）

感情心理学の研究手法に基づいた高度な分析を提供するタブです。研究・論文・学術的なレポート作成に活用できます。

---

### 理論的フレームワーク（3つのカード）

| 理論 | 提唱者 | 概要 |
|---|---|---|
| **Affect Dynamics** | Kuppens et al. (2010) | 感情の変動性・不安定性・慣性を定量化 |
| **Circumplex Model of Affect** | Russell (1980) | 感情を覚醒度×感情価の2次元で分類 |
| **Facial Action Coding System** | Ekman & Friesen (1978) | 顔の筋肉動作をAUで体系化 |

---

### AFFECT DYNAMICS — VARIABILITY（変動性比較）

全感情・Engagement・Valenceの「変動性（SD）」を棒グラフで比較します。

---

### AFFECT DYNAMICS — INERTIA（慣性比較）

全感情の「慣性（AR1）」を棒グラフで比較します。

---

### CORRELATION MATRIX（相関行列ヒートマップ）

全感情・Engagement・Valence間の相関係数を色付きのヒートマップで表示します。

- **赤色：** 正の相関
- **青色：** 負の相関
- **白色：** 無相関

---

### CIRCUMPLEX MODEL — QUADRANT ANALYSIS（象限分析）

感情状態を4象限に分類したフレーム数を表示します（特殊指標タブのCIRCUMPLEX MODELと同内容）。

---

### ACADEMIC INTERPRETATION（学術的解釈）

データから自動生成された学術的な解釈コメントを表示します。

---

### CSV レポート出力

画面右上の **「CSV出力」ボタン** で、学術セクションの全データを1ファイルにまとめてダウンロードできます。ファイル名は `ksdv_report_[元ファイル名].csv` です。

出力形式は以下の6〜7セクションで構成されます。

| セクション | 内容 |
|---|---|
| `## SESSION META` | ファイル名・総フレーム・録画時間・平均FPS・顔検出率・感情検出率 |
| `## EMOTION STATISTICS` | 9感情の mean / std / min / max / median / Q25 / Q75 |
| `## SPECIAL METRICS STATISTICS` | engagement / valence / attention の同統計量 |
| `## AFFECT DYNAMICS` | 11指標すべての variability_SD / instability_MSSD / inertia_AR1 / range / mean_absolute_change |
| `## CORRELATION MATRIX` | 感情×感情・特殊指標のピアソン相関行列 |
| `## CIRCUMPLEX MODEL (Russell 1980)` | 4象限のフレーム数 |
| `## CHANGE POINTS`（変化点がある場合のみ） | 時刻（秒）・感情・変化量・方向 |

> **用途：** 統計ソフト（R / Python / SPSS）への読み込み・論文への数値転記に利用できます。BOM付きCSVのためExcelでそのまま開けます。

---

## 7. アクションユニット（Action Units）

顔の筋肉動作（アクションユニット、AU）を分析するタブです。FACSシステムに基づき、どの表情筋がどのくらい活動したかを定量化します。

> **アクションユニット（AU）とは：** 顔の特定の筋肉グループの動きを名称で管理したもの。KSDVでは以下の22種類を追跡します：inner brow raise / brow raise / brow furrow / eye widen / cheek raise / lid tighten / nose wrinkle / upper lip raise / dimpler / lip corner depressor / chin raise / lip pucker / lip stretch / lip press / mouth open / jaw drop / lip suck / eye closure / smile / smirk / blink / blink rate

---

### AU MEAN ACTIVATION（AU別平均活性化値）

各アクションユニットの平均活性化値を棒グラフで表示します。

- **横軸：** AUの名称（smile、blink、brow furrowなど）
- **縦軸：** 平均活性化値（0〜100%）
- 棒が高いAU → セッション中に頻繁に活動した表情筋
- 例：「smile が最も高い」→ 微笑みに関わる筋肉が最もよく動いていた

---

### AU ACTIVITY RATE（AU活動率）

各AUが「一定以上の活性化を示したフレームの割合」を棒グラフで表示します。

- 例：「blink 活動率 85%」→ セッションの85%の時間で瞬きが検出された

---

### AU REFERENCE TABLE（AUリファレンス表）

全AUの詳細情報を一覧表で表示します。

| 列名 | 内容 |
|---|---|
| **AU名称** | アクションユニットの英語名称 |
| **動作の説明** | その筋肉が動いたときの表情の変化 |
| **平均値** | セッション中の平均活性化値 |
| **活動率** | 有意に活動していたフレームの割合 |

---

### HEAD POSE ANALYSIS（頭部姿勢分析）

頭の向き（Pitch・Yaw・Roll）の統計を表示します。

| 指標 | 意味 | 例 |
|---|---|---|
| **Pitch（上下）** | 頭の上下方向の傾き（プラス=上向き、マイナス=下向き） | mean=-2.3° → わずかに下向き |
| **Yaw（左右）** | 頭の左右方向の向き（プラス=右向き、マイナス=左向き） | mean=1.1° → ほぼ正面 |
| **Roll（傾き）** | 頭の左右への傾き（プラス=右傾き、マイナス=左傾き） | mean=0.5° → ほぼ垂直 |

---

### HEAD MOTION EVENTS（頭部動作検知）

セッション中に発生した頭部の意味ある動作イベントを自動検知して一覧表示します。

| 動作タイプ | 意味 |
|---|---|
| **nod（うなづき）** | Pitchの急激な変化 |
| **shake（首振り）** | Yawの急激な変化 |
| **tilt（首傾げ）** | Rollの急激な変化 |

各イベントには開始時刻・終了時刻・最大変化量（度）が記録されます。

---

## 8. UXリサーチ（UX Research）

製品・UXリサーチ向けに設計された複合スコアと感情パターン分析を提供するタブです。

---

### UXスコア（5つの複合指標）

| 指標 | 計算式 | 意味 |
|---|---|---|
| **フラストレーション指数** | confusion×0.4 + anger×0.3 + sadness×0.2 + disgust×0.1 | ユーザーの困難・ストレス度合い |
| **デライト指数** | joy×0.5 + surprise×0.3 + sentimentality×0.2 | ユーザーの喜び・満足度合い |
| **エンゲージメント品質** | engagement × max(0, 1 - confusion×2) | 混乱を除いた純粋な関与度 |
| **認知負荷推定** | confusion×0.6 + brow_furrow_active_pct/100×0.4 | 思考の重さ・情報処理負荷 |
| **総合UXスコア** | 上記の合成スコア（0〜100） | セッション全体のUX品質 |

各指標にはLOW/MED/HIGHの段階評価バッジが表示されます。

---

### 感情ヒートマップ

10秒ウィンドウ×感情の2次元ヒートマップです。「どの時間帯にどの感情が強く出たか」をひと目で確認できます。

---

### フリクション＆デライトタイムライン

感情変化点（change_points）を「フリクション（ネガティブ方向の急変）」「デライト（ポジティブ方向の急変）」に色分けして時系列表示します。フィルターでフリクションのみ・デライトのみを絞り込めます。

---

### タスク別サマリー

時系列タブで登録したイベント（タスク区間）ごとに、感情スコアとUXスコアを集計比較します。「どのタスク中に最もフラストレーションが高かったか」「どのタスクでデライトが最大だったか」を定量的に把握できます。

---

## 9. セッション比較（Comparison）

2つのCSVセッションを並べてA/B比較するタブです。ダッシュボード右上の「＋比較」ボタンから第2CSVをアップロードすると表示されます。

---

### セッションメタデータ比較

2セッションの基本情報を並列カードで表示します。

| 表示項目 | 意味 |
|---|---|
| **総フレーム** | 分析対象のデータ行数 |
| **録画時間** | データの合計時間（秒） |
| **平均FPS** | 1秒あたりの平均フレーム数 |
| **顔検出率** | 顔データが存在したフレームの割合 |

---

### 感情スコア平均値の比較（EMOTION MEANS）

9種類の非ニュートラル感情について、セッションA・Bの平均スコアを並列棒グラフで比較します。

---

### 特殊指標比較（SPECIAL METRICS） と 象限分布（CIRCUMPLEX MODEL）

横並びで2つのグラフを表示します。

- **特殊指標比較：** Engagement・Valence・AttentionのA/B平均値を棒グラフで比較
- **感情状態の象限分布：** Circumplex象限（高覚醒×高/低Valence・低覚醒×高/低Valence）のフレーム割合（%）を横棒グラフで比較

---

### 感情変動性レーダー（AFFECT DYNAMICS — VARIABILITY）

主要6感情（怒り・喜び・恐怖・悲しみ・驚き）とEngagementの「変動性（SD）」を2セッション重ね合わせのレーダーチャートで比較します。外側に広がるほど感情の起伏が大きいセッションです。

---

### Engagement 時系列オーバーレイ（ENGAGEMENT TIMELINE OVERLAY）

2セッションのEngagement時系列を、時間軸を0〜100%に正規化して重ね合わせ表示します。セッション長が異なっても波形パターンを直接比較できます。セッションBは破線で表示されます。

---

### 感情差分ランキング（DIFFERENCE TABLE）

|B-A|の差が大きい順に感情を並べ、どの感情でA・Bの差が顕著かを数値とバーで確認できます。正の差（B>A）はBカラー、負の差（A>B）はAカラーで表示されます。

---

### セッション間 統計的検定（STATISTICAL COMPARISON）

2セッションの全フレームを2標本として統計検定を実施します。画面上部のトグルで検定手法を切り替えられます。

**検定手法の選択：**

| 手法 | 特徴 | 適した場面 |
|---|---|---|
| **Welch t検定**（パラメトリック） | 正規分布を仮定。効果量は Cohen's d | 標本が大きく分布が概ね正規的な場合 |
| **Mann-Whitney U検定**（ノンパラメトリック） | 正規性を仮定しない。効果量はランク二列相関 r | データに外れ値が多い・正規性が疑わしい場合 |

**各検定の表示列：**

| Welch t検定 | Mann-Whitney U |
|---|---|
| 平均A / 平均B | 中央値A / 中央値B |
| t値 / df | U値 / z |
| p値 | p値 |
| Cohen's d | 効果量 r |
| 効果量カテゴリ | 効果量カテゴリ |
| 有意性マーク | 有意性マーク |

**有意性の表記：**

| マーク | 意味 |
|---|---|
| `***` | p < 0.001 |
| `**` | p < 0.01 |
| `*` | p < 0.05 |
| `n.s.` | p ≥ 0.05（有意差なし） |

**効果量の基準：**

- Welch（Cohen's d）：大（|d|≥0.8）/ 中（≥0.5）/ 小（≥0.2）/ 極小
- Mann-Whitney（r）：大（|r|≥0.5）/ 中（≥0.3）/ 小（≥0.1）/ 極小

**CSV出力ボタン：** 統計サマリーを1ファイルにまとめてダウンロードできます。ファイルは4セクション構成です。

| セクション | 内容 |
|---|---|
| `## COMPARISON META` | セッションA/Bのファイル名・出力日時 |
| `## DESCRIPTIVE STATISTICS` | 各感情の平均・中央値・SD・標本数（A/B） |
| `## WELCH T-TEST` | 全感情のWelch t検定結果（t値・df・p値・Cohen's d・効果量・有意性） |
| `## MANN-WHITNEY U TEST` | 全感情のMann-Whitney U検定結果（U・z・p値・効果量r・効果量・有意性） |

---

## 補足：数値の単位について

| 単位 | 意味 |
|---|---|
| **%（パーセント）** | 感情スコア・Engagement・Valence・Attentionのスコア |
| **frames（フレーム）** | データの行数（1フレーム = 1回の計測） |
| **sec（秒）** | 時間 |
| **AR1** | 自己回帰係数（慣性）。0〜1の値で、1に近いほど持続性が高い |
| **SD** | 標準偏差（Standard Deviation）。ばらつきの大きさ |
| **MSSD** | 平均二乗逐次差（Mean Square Successive Difference）。急激な変化の頻度 |
| **°（度）** | 頭部姿勢の角度 |

---

---

## 付録：学術研究・研究開発での活用ガイド

> このセクションは、研究論文への活用・査読対応を念頭に置いた補足情報です。数値の根拠・方法論の記述例・査読で問われやすい論点をまとめています。

---

### A. 推奨されるデータ品質基準

分析結果を論文に使用する前に、以下の基準を満たしているか確認してください。KEY INSIGHTSが自動的に警告を発する閾値も合わせて示します。

| 指標 | 推奨値 | KSDVの自動警告閾値 |
|---|---|---|
| **顔検出率** | ≥ 85% | < 70%（KEY INSIGHTSでアラート） |
| **感情検出率** | ≥ 60% | < 50%（KEY INSIGHTSで注意） |
| **フレームレート** | ≥ 15 fps | — （動態指標 AR1/MSSD の信頼性に直接影響） |
| **ベースライン区間の長さ** | 15〜30秒以上 | 短い・ばらつきが大きい場合は BASELINE SETTINGS に警告が出る |

> **注意：** 顔検出率が低い場合、感情スコアが0のフレームが混入し、平均値や動態指標が過小評価される可能性があります。論文には顔検出率を必ず記載してください。

---

### B. Affect Dynamics（感情動態指標）の解釈目安

#### AR1（慣性）の解釈

AR1（1ラグ自己相関係数）は感情状態の「持続しやすさ」を示します。−1〜+1の範囲を取ります。

| AR1の範囲 | 解釈 |
|---|---|
| **0.85 以上** | 感情状態が硬直しやすい。一度変わると長く持続する傾向 |
| **0.50〜0.85** | 中程度の持続性 |
| **0.30〜0.50** | 普通の変化しやすさ |
| **0.30 未満** | 感情が移ろいやすい・反応的（刺激に素早く反応して変化） |
| **負値** | 振動パターン（↑↓↑↓と交互に変化する） |

> 閾値 0.85 / 0.30 は、KSDVのKEY INSIGHTSエンジンが「高惰性」「移ろいやすい」と判定する基準値でもあります。

#### MSSD と SD² の比較（不安定性の解釈）

MSSD（平均二乗連続差）は、単独では解釈しにくいため、変動性（SD²）と比較するのが有効です。

| 比較 | 解釈 |
|---|---|
| **MSSD ≈ SD²** | 変化がランダムウォーク的（規則性のない揺れ） |
| **MSSD ＞ SD²** | 急激な変化が頻発（突発的な感情変化が多い） |
| **MSSD ＜ SD²** | 変化が緩やか（感情が滑らかに推移） |

> **参考文献：** Kuppens, P., et al. (2010)。同論文ではこれらの指標を組み合わせて感情の動態パターンを記述しています。

---

### C. ベースライン補正の方法論的記載

#### 補正モードの選択指針

| モード | 使う場面 | 値域 |
|---|---|---|
| **deviation（偏差）** | 標準的な実験研究。方向性（正/負）を保持して分析したい場合 | 負値あり |
| **lift（変化率%）** | 「ベースラインから何%増減したか」を個人間比較したい場合 | ±% |
| **zscore** | 個人間の比較・標準化が必要な場合。ベースラインのSD < 0.05 の場合は「—」表示 | SD単位 |

> **lift モードの注意：** Valenceは符号付き指標（中立=50%）のため変化率計算が成立せず、常に「—」と表示されます。Valenceの補正にはdeviation/zscoreを使用してください。

#### 論文Methodsセクションへの記述例

deviation modeの場合：
> 測定開始前30秒間（t = 0〜30 s）を安静時ベースライン区間として設定し、各感情スコアおよびEngagement・Valence・Attentionからベースライン平均値を減算した（deviation mode）。ベースライン中心値には算術平均を使用した（Kuppens et al., 2010）。

z スコアモードの場合：
> 安静時ベースライン区間（t = 0〜30 s）の平均（μ）と標準偏差（σ）を算出し、各フレームの感情スコアをz変換した（z = (x − μ) / σ）。σ < 0.05 のスコアは計算が不安定なため除外した。

---

### D. 統計検定の選択指針と多重比較の注意

#### 検定手法の選択

| 状況 | 推奨検定 |
|---|---|
| 標本が大きく（各群 n ≥ 30）、分布が概ね正規的 | **Welch t検定** |
| 外れ値が多い・正規性が疑わしい・標本が小さい | **Mann-Whitney U検定** |

感情スコアは一般に右歪み（低スコア側に値が集中する）であるため、Mann-Whitney U検定を標準として採用し、結果の頑健性確認にWelch t検定を並記する方法も有効です。

#### ⚠️ 多重比較補正について（重要）

KSDVは9つの感情を**補正なしで独立に検定**します。論文として発表する場合は、研究者自身で以下のいずれかの補正を適用してください。

| 補正方法 | 特徴 | 修正後有意水準（9感情の場合） |
|---|---|---|
| **Bonferroni補正** | 保守的。確実に第1種過誤を抑制 | α' = 0.05 / 9 ≈ **0.0056** |
| **Holm法** | Bonferroniより検出力が高い | 各p値を小さい順に調整 |
| **FDR補正（Benjamini-Hochberg法）** | 探索的研究向け。偽陽性率を制御 | 推奨FDR < 0.05 |

#### 論文Methodsへの記述例

> 各感情スコアのセッション間差はWelch's t検定（両側）で検定し、多重比較に対してBonferroni補正を適用した（α' = .05/9 = .006）。効果量はCohen's dで評価した（小: d ≥ .2, 中: d ≥ .5, 大: d ≥ .8; Cohen, 1988）。正規性が確認できない場合はMann-Whitney U検定（効果量: rank-biserial r, 小: r ≥ .1, 中: r ≥ .3, 大: r ≥ .5）を代替的に用いた。

---

### E. 査読で問われやすい論点と回答指針

| 問い | 回答指針 |
|---|---|
| **変化点検出の 2.5σ 閾値の根拠は？** | 信号処理・異常検知の標準値（グローバルSDの2.5倍）。現バージョンは固定値のため、論文では「探索的な検出」として記述し、閾値の感度分析（例: 2.0σ・3.0σでの比較）を補足することを推奨 |
| **Engagement を覚醒度（Arousal）代理指標として使う根拠は？** | Affectiva社のモデル定義に準拠。ただしAffectivaの覚醒度とRussell (1980)の理論的Arousalは完全に等価ではないため、外部妥当性については論文中で言及することを推奨 |
| **UX スコアの重みの根拠は？** | 当システム固有の経験的設計値（例: フラストレーション指数 = confusion×0.4 + anger×0.3 + …）。論文では「参考指標・探索的分析」として位置づけ、感情スコアの生値と並記することを推奨 |
| **Circumplex モデルはベースライン補正の影響を受けるか？** | 受けない。絶対値スケール（0〜100%）を前提とした計算のため補正対象外。補正有効中は画面に「補正対象外（絶対値）」バッジが表示される |
| **多重比較補正はアプリに実装されているか？** | 実装されていない。セクションDの方法で研究者が補正を適用すること |
| **感情スコアの信頼性・妥当性の根拠は？** | Affectiva（現 iMotions）の計測エンジンの精度に依存。論文にはAffectiva社の検証資料・先行研究を引用し、使用したモデルのバージョンを明記すること |
| **データの正規性はどう確認するか？** | 感情スコアは一般に右歪み。KSDVの記述統計表（mean/median/SD）でmean >> medianなら歪みを疑い、Mann-Whitney U検定を採用すること |

---

### F. 結果の論文記載例

以下は典型的な記述例です。数値はサンプルです。

**感情統計の記述例（ResultsまたはData section）：**
> 喜び（M = 23.4%, SD = 12.3%）が最も多く出現し、セッション中45.2%のフレームで支配的感情として検出された。感情多様性は7種類であった。

**Affect Dynamicsの記述例：**
> Engagementの慣性（AR1 = 0.82）は高く、一度高まった集中状態が持続する傾向が確認された（Kuppens et al., 2010）。不安定性（MSSD = 14.3）は変動性の二乗（SD² = 75.7）より小さく、急激な変化よりも緩やかな推移が支配的であった。

**ベースライン補正後の記述例（Valence, deviationモード）：**
> ベースライン補正後のValence（平均偏差 = +8.3 ポイント）は全体的にポジティブ方向への偏りを示した。なおliftモードはValenceの値域特性（符号付き中立値）により採用しなかった。

**統計検定の記述例（ComparisonセクションのCSV出力を使用する場合）：**
> セッションAとBの喜びスコアに有意差が認められた（Welch's t(4532) = 3.21, p = .001, d = 0.48 [中程度]; Mann-Whitney U = 12,430, z = 3.08, p = .002, r = .31 [中程度]）。

---

### G. 引用すべき主要文献

| 文献 | 対応する機能 |
|---|---|
| Kuppens, P., Allen, N. B., & Sheeber, L. B. (2010). Emotional inertia and psychological maladjustment. *Psychological Science*, 21(7), 984–991. | Affect Dynamics（AR1・MSSD・SD）・ベースライン補正 |
| Russell, J. A. (1980). A circumplex model of affect. *Journal of Personality and Social Psychology*, 39(6), 1161–1178. | Circumplex Model（4象限分類） |
| Ekman, P., & Friesen, W. V. (1978). *Facial Action Coding System: A technique for the measurement of facial movement*. Consulting Psychologists Press. | Action Units（AU分析） |
| Cohen, J. (1988). *Statistical power analysis for the behavioral sciences* (2nd ed.). Lawrence Erlbaum Associates. | 効果量の解釈基準（Cohen's d） |

---

*このドキュメントはKSDV v2.1（2026年6月）に基づいて作成されています。*
